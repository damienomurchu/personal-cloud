services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    restart: unless-stopped

    ports:
      - "127.0.0.1:3001:3001"

    volumes:
      - ${HOME}/personal-cloud-data/uptime-kuma:/app/data

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"